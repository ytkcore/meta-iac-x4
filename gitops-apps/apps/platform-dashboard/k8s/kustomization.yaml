apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: platform-dashboard-html
    namespace: apps
    files:
      - ../src/index.html
      - ../src/style.css
      - ../src/app.js
