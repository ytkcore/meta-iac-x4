################################################################################
# 55-rancher Stack - Minimal Configuration (External TLS Termination)
#
# 전제:
# - Ingress Service 앞단 Public NLB에서 ACM으로 TLS 종료
# - Rancher는 external TLS termination 모드(모듈 default)로 설치
#
# 사용법(권장):
#   cp terraform.tfvars.example dev.auto.tfvars   # 자동 로드
#   vi dev.auto.tfvars  # domain만 수정
#
# bootstrap_password는 파일에 저장하지 말고 환경변수로 주입 권장:
#   aws-vault exec <profile> -- env TF_VAR_bootstrap_password='ChangeMe!SecurePassword123' \
#     make apply ENV=dev STACK=55-rancher
################################################################################

# 필수: 도메인 (모듈 기본 FQDN: rancher.<domain>)
domain = "example.com"

# (옵션) 파일에 저장하지 말 것 (권장)
# bootstrap_password = "ChangeMe!SecurePassword123"
